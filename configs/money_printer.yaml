# configs/money_printer.yaml
# money_printer strategy configuration — USD-base, grow dollars.

pair: XBTUSD
strategy: money_printer

# Position sizing caps
max_long: 0.25    # max 25% of portfolio long
max_short: 0.15   # max 15% of portfolio short (BTC has long-term upward bias)

# Trading costs
fee_rate: 0.0026  # 0.26% taker fee (Kraken)

# Kill switches (per-strategy, evaluated independently of portfolio-level)
daily_dd_limit: -0.05   # -5%
total_dd_limit: -0.15   # -15%
